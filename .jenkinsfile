pipeline{
    agent any
    stages {
        stage ("show dir"){
            steps{
                script{
                    def commitHash = sh(script: 'git rev-parse HEAD', returnStdout: true).trim()
                    sh "docker-compose up"
                    sh "docker images"
                }                
            }
        }
    }
}