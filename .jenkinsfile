pipeline{
    agent any
    stages {
        stage ("show dir"){
            steps{
                commitHash = sh(script: 'git rev-parse HEAD', returnStdout: true).trim()
                sh "docker build -t carstore:$commitHash ."
                sh "docker images"
            }
        }
    }
}