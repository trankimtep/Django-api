pipeline{
    agent any
    stages {
        stage ("show dir"){
            steps{
                sh "export TAG=$(git rev-parse HEAD)"
                sh "docker build -t carstore:$TAG ."
                sh "docker images"
            }
        }
    }
}