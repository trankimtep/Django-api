pipeline{
    agent any
    stages {
        stage ("show dir"){
            steps{
                sh "echo $env.JOB_NAME"
                // script{
                //     withDockerRegistry(credentialsId: 'dockerhub', url: 'docker.io') {
                //         def commitHash = sh(script: 'git rev-parse HEAD', returnStdout: true).trim()
                //         sh "docker build -t trankimtep/carstore:$commitHash"
                //     }
                    
                    
                // }                
            }
        }
    }
}